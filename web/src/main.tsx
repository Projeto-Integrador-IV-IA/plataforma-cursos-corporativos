/**
 * Ponto de entrada do frontend.
 *
 * Responsabilidades deste modulo:
 *  - montar a aplicacao React na raiz do documento;
 *  - envolver a aplicacao nos providers globais (roteador, cliente de dados,
 *    contexto de autenticacao);
 *  - importar os estilos globais.
 *
 * TODO(scaffolding): implementar a montagem quando App existir.
 */

export {};
