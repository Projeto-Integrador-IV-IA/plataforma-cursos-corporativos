/**
 * Providers globais da aplicacao.
 *
 * Previstos: cliente de dados (cache e revalidacao das chamadas ao gateway),
 * contexto de autenticacao (token e usuario corrente) e contexto de
 * notificacoes de processamento (RF17).
 *
 * TODO(scaffolding): implementar.
 */

export {};
