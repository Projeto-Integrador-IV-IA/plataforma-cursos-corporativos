/**
 * Componente raiz da aplicacao.
 *
 * Define o layout base (navegacao lateral + area de conteudo) e delega as
 * telas ao roteador. Exibe o indicador global de processamento da estruturacao
 * por IA (RF17).
 *
 * TODO(scaffolding): implementar.
 */

export {};
